{% extends 'template.twig' %}

{% block head %}
    {{ include('head.twig') }}
{% endblock %}

{% block content %}
    {% if (is_admin == true) and (task['id'] is defined) %}
        {% include('extensions/edit_task.twig') %}
    {% else %}
        {% include('extensions/create_task.twig') %}
    {% endif %}
{% endblock %}

{% block scripts %}
    {{ include('common_js.twig') }}
    <script src="/js/taskPreview.js"></script>
{% endblock %}
